---
version: 1
kind: component
meta:
  name: generated
  source:
    dir: .

lifecycle:
  verbs:
    - deploy
    - undeploy

parameters:
  - name: dns.domain
    env: DOMAIN_NAME
  - name: component.ingress.ssoFqdn

templates:
  files:
    - "templates/*.template"
